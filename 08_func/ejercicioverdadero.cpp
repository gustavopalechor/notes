#include <stdio.h>
#include <stdlib.h>

    int main(){
    char  a[] = "cuadrado",
        b[] = "triangulo",
        c[] = "circulo",
        char opcion[N];
    system("clear");
    system("toilet --gay -fpagga ELIGE LA OPCION");
    printf("1. %s\n2. %s\n3. %s\n", a, b, c);
    fgets(opcion, N, stdin); 
    return EXIT_SUCCESS;
}
