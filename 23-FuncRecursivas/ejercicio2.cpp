#include <stdio.h>
#include <stdlib.h>

#define N 0x20
typedef struct{
    int data[N];
    int cima;
}Pila;

double fraccion(int *coef)
void push(Pila *coef, num){
    coef->data[coef->cima] = dato;
    coef->cima++;
}
void pop(Pila *coef){
    return coef->data[--coef->cima];
}
    int main(){
    Pila coef;
    int dato;
    
    do{
        printf("coef: \n");
        scanf(" %i", dato);
        if(dato > 0)
          push(&coef, dato);
      }while(dato > 0);
    printf("fracciones continuas: ", )
    return EXIT_SUCCESS;
}
